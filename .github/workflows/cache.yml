name: CI Cache Demo

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/demo
          key: cache-2fb1932

      - name: prime-cache-2fb1932
        run: |
          echo "Cache hit: ${{ steps.cache.outputs.cache-hit }}"

      - name: 22f2000984@ds.study.iitm.ac.in
        run: echo "Email verification step"
